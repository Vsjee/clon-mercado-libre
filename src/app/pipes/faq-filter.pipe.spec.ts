import { FaqFilterPipe } from './faq-filter.pipe';

describe('FaqFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FaqFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
